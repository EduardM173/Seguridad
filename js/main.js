document.addEventListener('DOMContentLoaded',()=>{document.getElementById('year')?.append(new Date().getFullYear());document.querySelectorAll('[data-optional-image]').forEach(el=>{const image=new Image();image.onload=()=>{el.style.setProperty('--optional-image',`url("${image.src}")`);el.classList.add('has-image')};image.src=el.dataset.optionalImage});const cards=document.getElementById('section-cards');if(cards){cards.innerHTML=SITE_SECTIONS.slice(1).map(s=>`<a class="section-card reveal" href="${toUrl(s)}"><span class="section-card__number">${s.number}</span><span><i class="section-card__icon" data-lucide="${s.icon}"></i><h3>${s.name}</h3><p>${s.description}</p></span><i class="section-card__arrow" data-lucide="arrow-right"></i></a>`).join('')}const videos=[['Introducción a la seguridad','Espacio preparado para tu recurso audiovisual.'],['Scrum en la práctica','Espacio preparado para tu recurso audiovisual.'],['Modelado IDEF0','Espacio preparado para tu recurso audiovisual.']];const videoCards=document.getElementById('video-cards');if(videoCards)videoCards.innerHTML=videos.map(([title,text])=>`<article class="video-card reveal"><div class="video-card__media"><button type="button" aria-label="Reproducir ${title}" data-video-placeholder><i data-lucide="play"></i></button></div><div class="video-card__body"><h3>${title}</h3><p>${text}</p></div></article>`).join('');document.querySelectorAll('[data-video-placeholder]').forEach(b=>b.addEventListener('click',()=>alert('Agrega el youtubeId en js/main.js para habilitar este video.')));window.lucide?.createIcons();});
